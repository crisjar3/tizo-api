spring:
  flyway:
    locations: classpath:db/migration,classpath:db/local
  jpa:
    hibernate:
      ddl-auto: validate

tizo:
  demo:
    enabled: true
    tools-enabled: true
  rate-limit:
    enabled: false
  effects:
    scheduling-enabled: false

management:
  server:
    port: 0
